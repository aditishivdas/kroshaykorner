services:
  - type: web
    name: kroshaykorner-website
    env: docker
    plan: free
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: kroshaykorner-db
          property: connectionString

databases:
  - name: kroshaykorner-db
    plan: free
